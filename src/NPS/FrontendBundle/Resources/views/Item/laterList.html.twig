{% extends 'NPSFrontendBundle::layout.html.twig' %}

{% block content %}
    <div class="row-fluid">
        <div class="block span12 inbox">
            <div class="block-body">

                <table class="table">
                    <tbody>
                    {% for laterItem in items %}
                        {% set userItem = laterItem.userItem %}
                        {% set item = laterItem.userItem.item %}
                        <tr>
                            <td class="list-later-item-btn">
                                <i class="btn showLink icon-external-link" data-url="{{ url('mark_later_read', {'label_id': laterItem.later.id, 'later_item_id': laterItem.id}) }}"
                                   data-externallink="{{ item.link }}" data-id="{{ item.id }}" title="{{ '_Show_in_tab'|trans }}" ></i>
                                <a class="btn icon-tag dropdown-toggle" data-toggle="dropdown" href="#" title="{{ '_Set_label'|trans }}"></a>
                                <ul class="dropdown-menu">
                                    {% for label in labels %}
                                        <li><a class="addLabelAction" data-url="{{ url('item_add_label', {'user_item_id': userItem.id, 'label_id': label.id}) }}"
                                               data-id="{{ label.id }}">{{ label.name }}</a></li>
                                    {% endfor %}
                                </ul>
                            </td>
                            <td class="inbox-message">
                                <a id="itemText-id-{{ item.id }}" class="list-item{% if laterItem.unread %} bold{% endif %}" href="{{ url('item_later_view', {'label_id': laterItem.LaterId, 'later_item_id': laterItem.id}) }}">{{ item.title|raw }}</a>
                            </td>
                            <td class="center dateColumn">{{ item.humanDateAdd }}</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>
{% endblock %}